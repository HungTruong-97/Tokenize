import React from  'react';
import {
    View, Text, Image, TouchableOpacity
} from 'react-native';

const Portfolio = ()=>{
    return(
        <View style={{flex:1}}>
            <Text>Portfolio</Text>
        </View>
    );
}

export default Portfolio;