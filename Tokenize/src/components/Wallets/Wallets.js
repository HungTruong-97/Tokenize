import React from  'react';
import {
    View, Text, Image, TouchableOpacity
} from 'react-native';

const Wallets = ()=>{
    return(
        <View style={{flex:1}}>
            <Text>Wallets</Text>
        </View>
    );
}

export default Wallets;